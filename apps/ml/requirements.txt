# Pinned to the versions verified to load the committed model artifacts and serve
# correct predictions end-to-end. scikit-learn is especially version-sensitive (the
# champion pickle); bump it only after re-validating Predictor.predict() against the
# committed artifacts and re-running scripts/calibrate_conformal.py.
fastapi==0.136.3
uvicorn[standard]==0.48.0
lightgbm==4.6.0
catboost==1.2.10
optuna==4.9.0
scikit-learn==1.7.2
pandas==2.3.3
numpy==2.3.5
pyyaml==6.0.3
pyarrow==21.0.0
matplotlib==3.10.6
mlflow==3.13.0
dvc==3.67.1
category_encoders==2.9.0
prophet==1.3.0

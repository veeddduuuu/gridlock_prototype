{
  "name": "gridlock-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "docker compose up --build",
    "prepare": "husky"
  },
  "dependencies": {
    "nodemon": "^3.1.14"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
